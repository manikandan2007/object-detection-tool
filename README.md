# object-detection-tool
The system uses a deep learning model (YOLO – You Only Look Once) integrated with Python and OpenCV to analyze live video frames captured from a webcam or external camera.The tool automatically draws bound boxes around detected objects and labels them with their name &amp;confidence scores. It also counts the number of objects and records and logs it.
